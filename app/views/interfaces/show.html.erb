<p class="interface">
  <strong>Title:</strong>
  <%= @interface.name %>
</p>

<h2>Sections</h2>
<% @interface.sections.each do |section| %>
    <strong>
      Section:
      <%= link_to 'Destroy', interface_section_path(@interface, section),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></br>

    </strong>
    <p class="section">
    <%= section.name %>
  </p>

  <p class="section">
    <strong>Count:</strong>
    <%= section.plot_count %>
  </p>
<% end %>

<h2>Add a section:</h2>
<%= form_for([@interface, @interface.sections.build]) do |f| %>
 <p class="section">
   <%= f.label :name %><br>
   <%= f.text_field :name %>
 </p>
 <p class="section">
   <%= f.label :plot_count %><br>
   <%= f.text_field :plot_count %>
 </p>
 <p>
   <%= f.submit %>
 </p>
<% end %>

<%= link_to 'Edit', edit_interface_path(@interface) %> |
<%= link_to 'Back', interfaces_path %>
